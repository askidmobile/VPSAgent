# Секреты VPSAgent (~/.vpsagent/secrets.toml, права 0600)
#
# Значения соответствуют `api_key_ref` в config.toml.
# Для локальных серверов (vLLM, llama.cpp) ключ можно оставить пустым.

openai = "sk-..."
anthropic = "sk-ant-..."
omniroute = "or-..."
openrouter = "sk-or-..."
local = ""
